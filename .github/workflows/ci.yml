name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    name: Test (Go ${{ matrix.go }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        go: ["1.26"]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go }}
          cache: true

      - name: Verify spec submodule pin
        run: |
          cd specs
          tag=$(git describe --tags --exact-match 2>/dev/null || echo "")
          if [ -z "$tag" ]; then
            echo "::error::specs/ submodule is not pinned to a tag"
            exit 1
          fi
          echo "Spec pinned to: $tag"

      - name: Build
        run: go build ./...

      - name: Test (race + coverage)
        run: go test -race -coverprofile=coverage.out -covermode=atomic ./...

      - name: Coverage summary
        run: go tool cover -func=coverage.out | tail -n 20

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage.out

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
          cache: true
      - uses: golangci/golangci-lint-action@v6
        with:
          version: latest

  vuln:
    name: Vulnerability scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
          cache: true
      - name: govulncheck
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...

  conformance:
    name: Spec conformance (Linea-conformance)
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout Linea-core
        uses: actions/checkout@v4
        with:
          submodules: recursive
          path: linea-core
      - name: Checkout Linea-conformance
        uses: actions/checkout@v4
        with:
          repository: nisarul/Linea-conformance
          path: linea-conformance
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
          cache: true
      - name: Pin runner to this Linea-core SHA
        working-directory: linea-conformance/runner-go
        run: |
          go mod edit -replace github.com/nisarul/Linea-core=../../linea-core
          go mod tidy
      - name: Run conformance suite
        working-directory: linea-conformance/runner-go
        run: go test -count=1 ./...
